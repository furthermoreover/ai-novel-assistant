# 更新日志

本仓库所有用户可见的功能变更与修复，按时间倒序记录。

## 2026-09-09

### 修复：删除章节后 Agent 记忆残留（废稿被记忆）

- **问题**：删除章节时只删除章节本身，Agent 记忆库（剧情时间线、伏笔、角色"最近出场章节"）中该章号的记录全部残留，导致已删除的废稿仍被记忆、统计数字虚高。
- **修复**：
  - `delete_chapter` 现在级联清理该章记忆：删除该章时间线；删除埋设于该章的伏笔（来源消失）；回收于该章的伏笔恢复为"未回收"；角色卡"最近出场章节"指向已删章时置空。
  - 服务启动时自动执行全项目"孤儿记忆清理"（幂等），清除指向不存在章节的记忆记录，历史残留一键修复。

### 修复：重复章号导致章节计数虚高

- **问题**：重复点击"按规划新建章节"时，系统会再创建一个同章号的章节（含空文本），导致章节计数重复、同一章被分析两次（时间线/伏笔重复）。
- **修复**：`create_chapter` 检测到目标章号已存在时自动顺延到下一个可用编号，不再产生重复章。
- **数据修复**：对现有项目执行了一次性清理——删除重复章号中的废稿行、去重该章重复时间线、重新分析保留章。当前项目恢复为 28 章、时间线 28 条（每章一条）。

### 优化：章节分析提速

- 压缩分析提示词输出规模（事件 ≤5 条、人物只列有变化的 ≤6 个、设定 ≤3 条、伏笔各 ≤3 条、全 JSON ≤1500 token），单章分析耗时约减半，输出 token 同步下降。
- 全量同步由逐章串行改为 3 路并发分析（LLM 调用并行；写库串行化加锁，防止并发"查重→插入"产生重复人物卡），整体耗时约降至原来的 1/3。

### 修复（上一版 UI）

- 切换"人物 / 章节 / Agent 助手"页签后，AI 写作的"目标章节"不再重置回第 1 章。
- 从"写作技能 / 知识库"返回"我的小说"时，不再退出当前正在编辑的小说项目，而是回到工作台。

### 功能：主题配色定制

- 内置 6 套预设主题（墨色书卷 / 青瓷烟雨 / 鎏金夜宴 / 绛紫玄夜 / 赤焰燃夜 / 素笺明窗），支持自定义色板实时预览，选择持久化保存，刷新不丢失。
